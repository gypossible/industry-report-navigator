{
  "name": "industry-report-navigator",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "vite preview --host 127.0.0.1 --port 4173",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:reports": "node scripts/generate-industry-report-navigator.mjs",
    "build:pages": "npm run generate:reports && vite build && rm -rf docs && cp -R dist docs"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "description": "Chinese industry report navigator with GitHub Pages-ready static deployment"
}
